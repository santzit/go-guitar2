# Godot 4+ specific ignores — keep extension_list.cfg committed so GDExtension
# classes (RocksmithBridge, AudioEngine) register in headless/CI runs without
# requiring a full editor import first.
.godot/
!.godot/extension_list.cfg
.nomedia
*.uid

# Godot-specific ignores
.import/
export.cfg
export_credentials.cfg

# Imported translations (automatically generated from CSV files)
*.translation

# Mono-specific ignores
.mono/
data_*/
mono_crash.*.json

# Rust build artefacts (keep gdextension/bin/ tracked — pre-built binaries)
gdextension/target/
wget-log

# Godot binary (downloaded at runtime, not committed)
Godot_v4.4.1-stable_linux.x86_64
Godot_v4.4.1-stable_linux.x86_64.zip
g.zip
