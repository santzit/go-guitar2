[package]
name    = "audio-engine"
version = "0.1.0"
edition = "2021"
description = "RT audio engine: dedicated thread, lock-free ring buffers, transport clock, command queue"

[dependencies]
audio-mixer    = { path = "../audio-mixer" }
rtrb           = "0.3"
thread-priority = "1.1"
